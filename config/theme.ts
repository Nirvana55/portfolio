import { createTheme } from '@mui/material';

export const theme = createTheme({
	palette: {
		mode: 'dark',
		background: {
			default: '#1E2022',
			paper: '#25282A',
		},
	},
	typography: {
		fontFamily: "'Montserrat', sans-serif;",
	},
});
